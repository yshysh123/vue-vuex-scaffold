import Vue from 'vue'
import Vuex from 'vuex'
/* imports-placeholder */

const store = {
  modules: {
    /* stores-placeholder */
  },
  strict: process.env.NODE_ENV !== 'production',
}

Vue.use(Vuex)

export default new Vuex.Store(store)
